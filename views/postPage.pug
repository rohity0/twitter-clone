extends layouts/main-layout.pug

block content
  script. 
    var postId = "!{postId}"       
  .postContainer

  +createReplyModal(userName)

block script 
      script(src="/Functions/postPage.js") 